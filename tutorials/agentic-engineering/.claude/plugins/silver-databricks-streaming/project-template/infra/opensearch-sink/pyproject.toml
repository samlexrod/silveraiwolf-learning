[project]
name = "opensearch-sink"
version = "0.2.0"
description = "Sinks Gold tables from Databricks to OpenSearch via PySpark JDBC + opensearch-hadoop"
requires-python = ">=3.11"
dependencies = [
    "pyspark>=4.1.0",
]
