# Environment variables for PostgreSQL DMS CDC deployment
# Copy this file to .env and update with your actual values

# Database password (required)
# For production: Use a strong, unique password
# For tutorial: Can use default or set your own
DB_PASSWORD=your_secure_password_here

# Optional: Override other defaults
# STACK_NAME=postgres-dms-cdc
# ENVIRONMENT=dev
# REGION=us-east-1
# TEMPLATE_FILE=postgres-dms-cdc.yaml
