# Silverline Explorer — server config. Copy to .env and adjust if needed.
#
# All Databricks credentials (workspace URL + PAT) are entered through the
# browser UI after launch — no secrets belong in this file.

PORT=4000
